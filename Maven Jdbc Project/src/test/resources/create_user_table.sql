CREATE TABLE IF NOT EXISTS user (id_user LONG PRIMARY KEY AUTO_INCREMENT, login VARCHAR(50) UNIQUE, password VARCHAR(50), email VARCHAR(50) UNIQUE, name VARCHAR(50),surname VARCHAR(50),birthday DATE, role_id LONG, FOREIGN KEY (role_id) REFERENCES role(id_role) ON DELETE CASCADE ON UPDATE CASCADE);
